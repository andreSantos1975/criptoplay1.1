// src/app/resgatar-bonus/layout.tsx
import React from 'react';

export default function ResgatarBonusLayout({ children }: { children: React.ReactNode }) {
  return (
    <>
      {children}
    </>
  );
}
